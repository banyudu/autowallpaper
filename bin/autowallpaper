#!/usr/bin/env node
'use strict'

const program = require('commander')
const pkg = require('../package.json')

const version = pkg.version

program
  .version(version)
  .option('new <name>', 'scaffold a new project')
  .parse(process.argv)
